'''
This module provides a global version number that can be imported as an
module attribute in other modules of the SAUR project.
'''

__version_info__ = (1, 2, 0)
__version__ = '.'.join(map(str, __version_info__))
